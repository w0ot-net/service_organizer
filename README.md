# service_organizer
create files grouping together ip:port pairs of different services based on nmap oX output

also writes `results/urls.txt` with discovered HTTP/HTTPS URLs, excluding `httpapi`, `ssdp`, and `upnp` services
the goal is to feed that list into a screenshotter like eyewitness
